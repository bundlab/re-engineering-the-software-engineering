services:
  localstack:
    image: localstack/localstack:3.0
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3,iam
      - DEBUG=1
    volumes:
      - "localstack-data:/var/lib/localstack"

volumes:
  localstack-data: